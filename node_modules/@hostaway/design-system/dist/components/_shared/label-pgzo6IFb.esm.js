import { jsxs, jsx } from 'react/jsx-runtime';
import { HelpCircle } from '@untitledui/icons';
import './MyCustomIcon-BRHTusqN.esm.js';
import { Label as Label$1 } from 'react-aria-components';
import { T as Tooltip, a as TooltipTrigger } from './tooltip-l8auK4sw.esm.js';
import { c as cx } from './cx-DnIR8RkH.esm.js';

const Label = ({ isRequired, tooltip, tooltipDescription, className, ...props }) => {
    return (jsxs(Label$1
    // Used for conditionally hiding/showing the label element via CSS:
    // <Input label="Visible only on mobile" className="lg:**:data-label:hidden" />
    // or
    // <Input label="Visible only on mobile" className="lg:label:hidden" />
    , { "data-label": "true", ...props, className: cx('flex cursor-default items-center gap-0.5 text-sm font-medium text-secondary', className), children: [props.children, jsx("span", { className: cx('hidden text-brand-tertiary', isRequired && 'block', typeof isRequired === 'undefined' && 'group-required:block'), children: "*" }), tooltip && (jsx(Tooltip, { title: tooltip, description: tooltipDescription, placement: "top", children: jsx(TooltipTrigger
                // `TooltipTrigger` inherits the disabled state from the parent form field
                // but we don't that. We want the tooltip be enabled even if the parent
                // field is disabled.
                , { 
                    // `TooltipTrigger` inherits the disabled state from the parent form field
                    // but we don't that. We want the tooltip be enabled even if the parent
                    // field is disabled.
                    isDisabled: false, className: "cursor-pointer text-fg-quaternary transition duration-200 hover:text-fg-quaternary_hover focus:text-fg-quaternary_hover", children: jsx(HelpCircle, { className: "size-4" }) }) }))] }));
};
Label.displayName = 'Label';

export { Label as L };
//# sourceMappingURL=label-pgzo6IFb.esm.js.map
