'use strict';

var jsxRuntime = require('react/jsx-runtime');

// Map non-ISO country ids (e.g. `uk`) to ISO 3166-1 alpha-2 for asset lookup.
// Downstream apps may persist the original id, so translate only at render time.
const COUNTRY_ID_ALIASES = {
    uk: 'gb',
};
function aliasCountryId(id) {
    var _a;
    const lower = id.toLowerCase();
    const aliased = (_a = COUNTRY_ID_ALIASES[lower]) !== null && _a !== void 0 ? _a : lower;
    return aliased.toUpperCase();
}
const FlagIcon = ({ flag }) => {
    const code = aliasCountryId(flag);
    return (jsxRuntime.jsx("img", { src: `https://www.untitledui.com/images/flags/${code}.svg`, className: "size-4 max-w-none rounded-full", alt: `${code} flag` }));
};

exports.COUNTRY_ID_ALIASES = COUNTRY_ID_ALIASES;
exports.FlagIcon = FlagIcon;
exports.aliasCountryId = aliasCountryId;
//# sourceMappingURL=FlagIcon-BbhvmpcI.cjs.js.map
