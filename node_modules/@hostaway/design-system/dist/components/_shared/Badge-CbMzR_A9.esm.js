import { jsx } from 'react/jsx-runtime';
import { B as BadgeWithDot, a as BadgeWithIcon, b as Badge$1 } from './badges-ihdaM96E.esm.js';

function Badge(props) {
    const { color = 'gray', type = 'pill-color', variant = 'default', ...rest } = props;
    const badgeColor = color;
    if (variant === 'dot') {
        return (jsx(BadgeWithDot, { type: type, color: badgeColor, ...rest, children: props.children }));
    }
    if (variant === 'icon') {
        return (jsx(BadgeWithIcon, { type: type, color: badgeColor, ...rest, children: props.children }));
    }
    return (jsx(Badge$1, { type: type, color: badgeColor, ...rest, children: props.children }));
}

export { Badge as B };
//# sourceMappingURL=Badge-CbMzR_A9.esm.js.map
