import { jsx, Fragment } from 'react/jsx-runtime';
import { T as Tooltip, a as TooltipTrigger } from './tooltip-l8auK4sw.esm.js';

/**
 * Wraps `children` in a tooltip only when the consumer has detected truncation.
 * Pair with `useTruncation()` from `@/hooks/use-truncation`.
 */
function ConditionalTooltip({ text, isTruncated, children, }) {
    if (!isTruncated)
        return jsx(Fragment, { children: children });
    return (jsx(Tooltip, { title: text, placement: "top", children: jsx(TooltipTrigger, { isDisabled: false, className: "flex w-full min-w-0 cursor-default text-left", children: children }) }));
}

export { ConditionalTooltip as C };
//# sourceMappingURL=ConditionalTooltip-DT2TnX8y.esm.js.map
