import type { ReactNode } from 'react';
import type { DatePickerProps as AriaDatePickerProps, DateValue } from 'react-aria-components';
import { WithDataId } from '../../../types/withDataId';
export interface DatePickerFieldProps extends AriaDatePickerProps<DateValue>, WithDataId {
    /** Label text for the date picker */
    label?: string;
    /** Helper text displayed below the date picker */
    hint?: ReactNode;
    /** Label tooltip message */
    tooltip?: string;
    /** Label tooltip description */
    tooltipDescription?: string;
    /** Custom class name for the wrapper */
    className?: string;
    /** Whether the field is required */
    required?: boolean;
}
export declare function DatePickerField({ label, hint, tooltip, tooltipDescription, className, required, isRequired: isRequiredProp, isInvalid, ...props }: DatePickerFieldProps): import("react/jsx-runtime").JSX.Element;
