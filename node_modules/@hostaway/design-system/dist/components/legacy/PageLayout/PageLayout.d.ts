import { WithDataId } from '../../../types/withDataId';
export interface TabItem {
    label: string;
    to: string;
}
export interface PageLayoutProps extends WithDataId {
    children: React.ReactNode;
    title: string;
    tabs?: TabItem[];
    selectedTab?: TabItem['to'];
    onTabChange?: (tabTo: string) => void;
}
export declare function PageLayout({ children, title: _title, tabs, selectedTab, onTabChange, 'data-id': dataId, }: PageLayoutProps): import("react/jsx-runtime").JSX.Element;
