import type { DateRangePickerProps as AriaDateRangePickerProps, DateValue } from 'react-aria-components';
import { WithDataId } from '../../../types/withDataId';
import { DEFAULT_TEXTS } from './defaultTexts';
import { FieldWrapperProps } from '../../molecules/FieldWrapper/FieldWrapper';
export type DateRangePickerTexts = typeof DEFAULT_TEXTS;
export interface DateRangePickerProps extends AriaDateRangePickerProps<DateValue>, WithDataId, Omit<FieldWrapperProps, 'children'> {
    /** Custom formatter for displaying selected dates in the trigger button. */
    formatDate?: (date: Date) => string;
    /** The function to call when the apply button is clicked. */
    onApply?: () => void;
    /** The function to call when the cancel button is clicked. */
    onCancel?: () => void;
    /** Override default UI texts for i18n. */
    t?: DateRangePickerTexts;
    /** Override the default local timezone. */
    timeZone?: string;
}
export declare const DateRangePicker: ({ value: valueProp, defaultValue, onChange, onApply, onCancel, formatDate, t: textsProp, timeZone, "data-id": dataId, isInvalid, isRequired, label, hint, labelPosition, tooltip, description, id, ...props }: DateRangePickerProps) => import("react/jsx-runtime").JSX.Element;
