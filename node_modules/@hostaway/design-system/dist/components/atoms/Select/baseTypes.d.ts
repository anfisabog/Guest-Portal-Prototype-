import type { FC, ReactNode } from 'react';
export type SelectItemType = {
    id: string | number;
    label?: string | React.JSX.Element;
    renderLabel?: React.JSX.Element;
    avatarUrl?: string;
    isDisabled?: boolean;
    supportingText?: string;
    icon?: FC | ReactNode;
    /**
     * Extra strings to match against when the Select is `searchable`. Useful for
     * synonyms or localized aliases. Search is case-insensitive substring.
     */
    searchAliases?: string[];
};
export interface CommonProps {
    hint?: string;
    label?: string;
    tooltip?: string;
    placeholder?: string;
}
