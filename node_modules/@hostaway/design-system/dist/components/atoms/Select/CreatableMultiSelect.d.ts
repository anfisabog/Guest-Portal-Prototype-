import type { Key } from 'react-aria-components';
import type { SelectItemType } from './baseTypes';
import { WithDataId } from '../../../types/withDataId';
import { FieldWrapperProps } from '../../molecules/FieldWrapper/FieldWrapper';
export type CreatableMultiSelectItem<T extends Key = Key> = Omit<SelectItemType, 'id'> & {
    id: T;
};
export interface CreatableMultiSelectProps<T extends Key = Key> extends WithDataId, Omit<FieldWrapperProps, 'children'> {
    placeholder?: string;
    addOptionPlaceholder?: string;
    addOptionMaxLength?: number;
    searchPlaceholder?: string;
    items?: CreatableMultiSelectItem<T>[];
    /**
     * Fallback options for displaying selected values that may not be in `items`.
     * Useful when:
     * - Selected items are filtered out by search
     * - Options are paginated/lazy-loaded
     * - Async options haven't loaded yet
     *
     * These are NOT shown in the dropdown, only used for badge display.
     */
    preparedOptions?: CreatableMultiSelectItem<T>[];
    selectedKeys?: T[];
    defaultSelectedKeys?: T[];
    isDisabled?: boolean;
    isCreatable?: boolean;
    isRemovable?: boolean;
    isSearchable?: boolean;
    className?: string;
    popoverClassName?: string;
    onChange?: (selectedKeys: T[]) => void;
    onItemAdd?: (label: string) => void;
    onItemRemove?: (key: T, close: () => void) => void;
    onSearch?: (query: string) => void;
    onScrollEnd?: () => void;
    onOpenChange?: (isOpen: boolean) => void;
    t?: {
        addButton: string;
    };
    onBlur?: () => void;
}
declare const CreatableMultiSelect: <T extends Key = string>({ placeholder, addOptionPlaceholder, addOptionMaxLength, searchPlaceholder, items: initialItems, preparedOptions, selectedKeys: selectedKeysProp, defaultSelectedKeys, isDisabled, isCreatable, isRemovable, isSearchable, popoverClassName, onChange, onItemAdd, onItemRemove, onSearch, onScrollEnd, onOpenChange: onOpenChangeProp, t, "data-id": dataId, label, hint, isInvalid, isRequired, tooltip, description, id, fieldSize, labelPosition, onBlur, }: CreatableMultiSelectProps<T>) => import("react/jsx-runtime").JSX.Element;
export { CreatableMultiSelect };
