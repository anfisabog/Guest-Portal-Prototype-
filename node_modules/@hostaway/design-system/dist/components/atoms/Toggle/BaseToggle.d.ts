import type { ReactNode } from 'react';
import type { SwitchProps as AriaSwitchProps } from 'react-aria-components';
interface ToggleBaseProps {
    className?: string;
    isHovered?: boolean;
    isFocusVisible?: boolean;
    isSelected?: boolean;
    isDisabled?: boolean;
}
export declare const ToggleBase: ({ className, isHovered, isDisabled, isFocusVisible, isSelected, }: ToggleBaseProps) => import("react/jsx-runtime").JSX.Element;
export interface ToggleProps extends AriaSwitchProps {
    label?: string;
    hint?: ReactNode;
}
export declare const Toggle: ({ label, hint, className, ...ariaSwitchProps }: ToggleProps) => import("react/jsx-runtime").JSX.Element;
export {};
