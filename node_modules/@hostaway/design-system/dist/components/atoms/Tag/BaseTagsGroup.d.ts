import { type PropsWithChildren, type RefAttributes } from 'react';
import { type TagGroupProps as AriaTagGroupProps, type TagProps as AriaTagProps } from 'react-aria-components';
import { FlagTypes } from '../../../vendor/untitled-ui/base/badges/badge-types';
export interface TagItem {
    id: string;
    label: string;
    count?: number;
    avatarSrc?: string;
    avatarContrastBorder?: boolean;
    dot?: boolean;
    dotClassName?: string;
    isDisabled?: boolean;
    onClose?: (id: string) => void;
}
interface TagGroupProps extends Omit<AriaTagGroupProps, 'selectionMode' | 'disallowEmptySelection'>, RefAttributes<HTMLDivElement> {
    label: string;
    size?: 'sm' | 'md' | 'lg';
    /** @deprecated No longer affects rendering. Accepted for backwards compatibility with consumers still passing it. */
    selectionMode?: 'none' | 'single' | 'multiple';
    /** @deprecated No longer affects rendering. Accepted for backwards compatibility with consumers still passing it. */
    disallowEmptySelection?: boolean;
}
export declare const TagGroup: ({ label, size, selectionMode: _selectionMode, disallowEmptySelection: _disallowEmptySelection, children, ...otherProps }: TagGroupProps) => import("react/jsx-runtime").JSX.Element;
export declare const TagList: <T extends object>(props: import("react-aria-components").TagListProps<T> & React.RefAttributes<HTMLDivElement>) => React.ReactElement | null;
export interface TagProps extends AriaTagProps, RefAttributes<object>, Omit<TagItem, 'label' | 'id'> {
    flag?: FlagTypes;
}
export declare const Tag: ({ id, avatarSrc, avatarContrastBorder, dot, dotClassName, isDisabled, count, className, children, onClose, flag, }: PropsWithChildren<TagProps>) => import("react/jsx-runtime").JSX.Element;
export {};
