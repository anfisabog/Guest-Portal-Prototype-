import type { FC, ReactNode, RefAttributes } from 'react';
import type { ButtonProps as AriaButtonProps, MenuItemProps as AriaMenuItemProps, MenuProps as AriaMenuProps, PopoverProps as AriaPopoverProps, SeparatorProps as AriaSeparatorProps } from 'react-aria-components';
import { MenuTrigger as AriaMenuTrigger } from 'react-aria-components';
import { WithDataId } from '../../../../types/withDataId';
export interface DropdownItemRenderProps {
    /** Whether the item is currently disabled. */
    isDisabled: boolean;
    /** Whether the item is currently focused. */
    isFocused: boolean;
    /** Whether the item is currently selected. */
    isSelected: boolean;
    /** Whether the item is currently hovered. */
    isHovered: boolean;
    /** Whether the item is currently pressed. */
    isPressed: boolean;
    /** Whether the item is keyboard focused. */
    isFocusVisible: boolean;
}
interface DropdownItemProps extends AriaMenuItemProps, WithDataId {
    /** The label of the item to be displayed. */
    label?: string;
    /** An addon to be displayed on the right side of the item. */
    addon?: string;
    /** If true, the item will not have any styles. */
    unstyled?: boolean;
    /** An icon to be displayed on the left side of the item. */
    icon?: FC<{
        className?: string;
    }>;
    /** Tooltip text to display on hover. */
    tooltip?: string;
    /**
     * Whether the menu should close when this item is selected.
     * @default true
     */
    closeOnSelect?: boolean;
    /**
     * Custom render function for the item content.
     * When provided, replaces the default item rendering.
     */
    renderItem?: (props: DropdownItemRenderProps) => ReactNode;
}
interface DropdownMenuProps<T extends object> extends AriaMenuProps<T> {
}
export interface DropdownPopoverProps extends AriaPopoverProps {
}
export declare const Dropdown: {
    Root: typeof AriaMenuTrigger;
    Popover: (props: DropdownPopoverProps) => import("react/jsx-runtime").JSX.Element;
    Menu: <T extends object>(props: DropdownMenuProps<T>) => import("react/jsx-runtime").JSX.Element;
    Section: <T extends object>(props: import("react-aria-components").MenuSectionProps<T> & React.RefAttributes<HTMLElement>) => import("react").ReactElement | null;
    SectionHeader: (props: import("react-aria-components").HeaderProps & React.RefAttributes<HTMLElement>) => React.ReactElement | null;
    Item: ({ label, children, addon, icon: Icon, unstyled, tooltip, renderItem, ...props }: DropdownItemProps) => import("react/jsx-runtime").JSX.Element;
    Separator: (props: AriaSeparatorProps) => import("react/jsx-runtime").JSX.Element;
    DotsButton: (props: {
        horizontal?: boolean;
    } & AriaButtonProps & RefAttributes<HTMLButtonElement>) => import("react/jsx-runtime").JSX.Element;
};
export {};
