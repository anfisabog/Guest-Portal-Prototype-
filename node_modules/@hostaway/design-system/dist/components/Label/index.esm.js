import { jsxs, jsx } from 'react/jsx-runtime';
import { Label as Label$1 } from 'react-aria-components';
import { c as cx } from '../_shared/cx-DnIR8RkH.esm.js';
import { T as Tooltip } from '../_shared/Tooltip-BtHtZ-Fm.esm.js';
import 'tailwind-merge';
import '../_shared/tooltip-l8auK4sw.esm.js';
import '@untitledui/icons';

const Label = ({ isRequired, tooltip, tooltipDescription, className, ...props }) => {
    return (jsxs(Label$1
    // Used for conditionally hiding/showing the label element via CSS:
    // <Input label="Visible only on mobile" className="lg:**:data-label:hidden" />
    , { "data-label": "true", ...props, className: cx('flex cursor-default items-center gap-0.5 text-sm font-medium text-secondary', className), children: [props.children, jsx("span", { className: cx('hidden text-brand-tertiary', isRequired && 'block', typeof isRequired === 'undefined' && 'group-required:block'), children: "*" }), tooltip && (jsx(Tooltip, { title: tooltip, description: tooltipDescription, placement: "top", isHelpIconTrigger: true, 
                // TooltipTrigger inherits the disabled state from the parent form
                // field, but we want the tooltip enabled even if the field is disabled.
                triggerProps: { isDisabled: false }, "data-id": "label-tooltip" }))] }));
};
Label.displayName = 'Label';

export { Label };
//# sourceMappingURL=index.esm.js.map
