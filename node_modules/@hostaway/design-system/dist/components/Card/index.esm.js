import { jsx, jsxs } from 'react/jsx-runtime';
import { c as cx } from '../_shared/cx-DnIR8RkH.esm.js';
import 'tailwind-merge';

const Root = ({ variant = 'bordered', size = 'md', color = 'default', className, ...props }) => {
    return (jsx("div", { ...props, className: cx(
        // Base styles
        'overflow-hidden rounded-xl', color === 'default' && 'bg-primary', color === 'secondary' && 'bg-secondary', 
        // Variant styles
        variant === 'default' && 'shadow-xs', variant === 'bordered' && 'ring-1 ring-secondary', variant === 'shadow' && 'shadow-lg', 
        // Size styles
        size === 'sm' && 'rounded-lg', size === 'md' && 'rounded-xl', size === 'lg' && 'rounded-2xl', className) }));
};
const Header = ({ className, variant, ...props }) => {
    return (jsx("div", { ...props, className: cx(cx('p-4', variant !== 'secondary' && ['border-b border-secondary bg-primary'], className)) }));
};
const Body = ({ className, ...props }) => {
    return (jsx("div", { ...props, className: cx('flex flex-col px-4 py-2 gap-2', className) }));
};
const Footer = ({ className, ...props }) => {
    return (jsx("div", { ...props, className: cx('border-t border-secondary bg-utility-gray-50 px-6 py-4', className) }));
};
const Grid = ({ title = undefined, subtitle = undefined, children, className = '', }) => {
    return (jsxs("div", { className: cx('flex flex-wrap gap-4', className), children: [title && (jsxs("div", { className: "flex flex-col items-start w-full gap-1", children: [jsx("div", { className: "text-lg", children: title }), subtitle && (jsx("div", { className: "text-sm font-inter text-default-400", children: subtitle }))] })), children] }));
};
const Card = Root;
Card.Header = Header;
Card.Body = Body;
Card.Footer = Footer;
Card.Grid = Grid;

export { Card, Grid };
//# sourceMappingURL=index.esm.js.map
