import type { Placement } from 'react-aria';
import { Dropdown as BaseDropdown, DropdownPopoverProps } from './base';
import { AvatarProps } from '../Avatar';
import { ChevronDown } from '@untitledui/icons';
import { ComponentPropsWithoutRef } from 'react';
import { WithDataId } from '../../../types/withDataId';
export type DropdownSectionItem = ComponentPropsWithoutRef<typeof BaseDropdown.Item> & {
    tooltip?: string;
};
type IconComponent = typeof ChevronDown;
interface AvatarTriggerProps {
    src?: string;
    status?: AvatarProps['status'];
    title?: string;
    subtitle?: string;
}
export interface DropdownProps extends WithDataId {
    label?: string;
    type: 'button' | 'icon' | 'avatar' | 'label';
    /** @default "md" */
    size?: 'sm' | 'md' | 'lg' | 'xl';
    icon?: IconComponent;
    shouldHideChevron?: boolean;
    avatar?: AvatarTriggerProps;
    sections: {
        items: DropdownSectionItem[];
    }[];
    placement?: DropdownPopoverProps['placement'];
    /** Whether the dropdown is disabled */
    isDisabled?: boolean;
    /** Tooltip text to display on hover */
    tooltip?: string;
    /** Placement of the tooltip */
    tooltipPlacement?: Placement;
    /** Styles for Dropodown Popover */
    popoverClassNames?: string;
}
export declare const Dropdown: ({ avatar, sections, label, type, size, icon, shouldHideChevron, "data-id": dataId, placement, isDisabled, tooltip, tooltipPlacement, popoverClassNames, }: DropdownProps) => import("react/jsx-runtime").JSX.Element;
export {};
