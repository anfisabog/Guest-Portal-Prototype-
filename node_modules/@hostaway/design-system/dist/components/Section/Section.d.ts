import { WithDataId } from '../../../types/withDataId';
export type SectionProps = {
    title: string;
    subtitle?: React.ReactNode;
    buttons?: React.ReactNode;
    children: React.ReactNode;
    className?: string;
    responsive?: boolean;
} & WithDataId;
export declare function Section({ title, subtitle, buttons, children, className, responsive, 'data-id': dataId, }: SectionProps): import("react/jsx-runtime").JSX.Element;
