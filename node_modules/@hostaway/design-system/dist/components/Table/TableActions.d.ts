import type { FC, ReactNode } from 'react';
import { WithDataId } from '../../../types/withDataId';
type IconComponent = FC<{
    className?: string;
}>;
export interface TableAction {
    dataId: string;
    label: string;
    icon: IconComponent;
    onClick: () => void;
    isDisabled?: boolean;
    tooltip?: string;
}
export interface TableActionSeparator {
    type: 'separator';
}
export interface TableActionCustom {
    type: 'custom';
    render: () => ReactNode;
    dataId: string;
}
export type TableActionItem = TableAction | TableActionSeparator | TableActionCustom;
export interface TableActionsProps extends Partial<WithDataId> {
    actions: TableActionItem[];
    /** Accessible label for the action group. Defaults to "Row actions". */
    'aria-label'?: string;
    maxInlineActions?: number;
}
export declare function TableActions({ actions, 'data-id': dataId, 'aria-label': ariaLabel, maxInlineActions, }: TableActionsProps): import("react/jsx-runtime").JSX.Element;
export {};
