'use strict';

var jsxRuntime = require('react/jsx-runtime');
var h = require('react');

function FieldGroup({ children, className = '', ...rest }) {
    const childrenArray = h.Children.toArray(children);
    const childCount = childrenArray.length;
    const baseClasses = 'flex w-full gap-4';
    // Auto-determine width based on number of children
    const getWidthClass = (count) => {
        switch (count) {
            case 1:
                return '[&>*]:w-full';
            case 2:
                return '[&>*]:w-1/2';
            case 3:
                return '[&>*]:w-1/3';
            case 4:
                return '[&>*]:w-1/4';
            default:
                // For 5+ children, use flex-1 for equal distribution
                return '[&>*]:flex-1';
        }
    };
    const widthClass = getWidthClass(childCount);
    const combinedClasses = `${baseClasses} ${widthClass} ${className}`.trim();
    return (jsxRuntime.jsx("div", { className: combinedClasses, ...rest, children: children }));
}

exports.FieldGroup = FieldGroup;
//# sourceMappingURL=index.cjs.js.map
