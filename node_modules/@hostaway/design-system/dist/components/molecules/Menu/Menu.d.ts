import { WithDataId } from '../../../types/withDataId';
export type MenuOption = {
    label: string;
    value: string;
};
export type MenuGroup = {
    label: string;
    items: MenuOption[];
};
export type MenuProps = {
    groups: MenuGroup[];
    onSelect: (item: MenuOption, groupLabel: string) => void;
    selectedValues?: Set<string>;
    isOpen?: boolean;
    onOpenChange?: (isOpen: boolean) => void;
    withSearch?: boolean;
    searchPlaceholder?: string;
    children: React.ReactNode;
    className?: string;
    placement?: 'bottom' | 'bottom start' | 'bottom end' | 'top' | 'top start' | 'top end';
    closeOnSelect?: boolean;
} & WithDataId;
export declare function Menu({ groups, onSelect, selectedValues, isOpen: controlledIsOpen, onOpenChange, withSearch, searchPlaceholder, children, className, placement, closeOnSelect, 'data-id': dataId, }: MenuProps): import("react/jsx-runtime").JSX.Element;
