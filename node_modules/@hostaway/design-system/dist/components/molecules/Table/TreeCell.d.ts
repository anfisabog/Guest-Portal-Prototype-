import type { WithDataId } from '../../../types/withDataId';
import type { TableItemMeta } from './_hooks/useTableItems';
import type { TableRowIdentifier } from './_types';
interface TreeCellProps extends WithDataId {
    meta: TableItemMeta;
    itemId: TableRowIdentifier;
    collapsible: boolean;
    isCollapsed: boolean;
    toggleExpanded?: (key: TableRowIdentifier) => void;
    children: React.ReactNode;
    treeLineLeft?: string;
}
export declare function TreeCell({ meta, itemId, collapsible, isCollapsed, toggleExpanded, children, treeLineLeft, 'data-id': dataId, }: TreeCellProps): import("react/jsx-runtime").JSX.Element;
export {};
