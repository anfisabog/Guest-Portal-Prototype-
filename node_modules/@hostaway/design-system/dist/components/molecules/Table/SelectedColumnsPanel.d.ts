import { TableRow, ColDef } from './_types';
export type ColumnId = string | number;
export declare function SelectedColumnsPanel<TData extends TableRow>({ columns, columnOrder, onColumnOrderChange, texts, dataId, }: {
    columns: ColDef<TData>[];
    columnOrder?: Record<ColumnId, number>;
    onColumnOrderChange?: (newOrder: Record<ColumnId, number>) => void;
    texts: {
        columnsSelector: Record<string, string>;
    };
    dataId: string | undefined;
}): import("react/jsx-runtime").JSX.Element;
