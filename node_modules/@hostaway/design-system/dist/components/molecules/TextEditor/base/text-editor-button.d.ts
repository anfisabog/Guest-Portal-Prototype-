import { type ButtonProps } from 'react-aria-components';
interface EditorButtonProps extends ButtonProps {
    isActive?: boolean;
}
export declare const EditorButton: ({ isActive, isDisabled, className, children, ...props }: EditorButtonProps) => import("react/jsx-runtime").JSX.Element;
export {};
