import type { ComponentProps, ReactNode } from 'react';
import { FileIcon as FileTypeIcon } from '../../../icons';
import { WithDataId } from '../../../types/withDataId';
export interface FileUploadedProps extends WithDataId, Omit<ComponentProps<'div'>, 'onClick' | 'title'> {
    title?: string;
    fileName?: string;
    type?: ComponentProps<typeof FileTypeIcon>['type'];
    className?: string;
    fileIconVariant?: ComponentProps<typeof FileTypeIcon>['variant'];
    actions?: () => ReactNode;
    url?: string;
}
export declare const FileUploaded: ({ title, fileName, type, fileIconVariant, actions, className, "data-id": dataId, url, ...rest }: FileUploadedProps) => import("react/jsx-runtime").JSX.Element;
