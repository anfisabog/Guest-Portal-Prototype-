import { WithDataId } from '../../../types/withDataId';
import { type DropdownSectionItem } from '../../atoms/Dropdown';
export type BulkActionItem = DropdownSectionItem & WithDataId & {
    isLoading?: boolean;
};
export type BulkActionSection = {
    items: BulkActionItem[];
};
export interface BulkActionMenuLabels {
    /** Label for the selected count, e.g. "6 selected" */
    selected: string;
    /** Tooltip text for the close button */
    clearAll: string;
}
export interface BulkActionMenuProps extends WithDataId, React.ComponentProps<'div'> {
    /** Action sections matching Dropdown's sections type, extended with isLoading */
    actions: BulkActionSection[];
    /** Translatable labels */
    labels: BulkActionMenuLabels;
    /** Maximum number of visible action buttons before overflow (default: 3) */
    maxVisibleActions?: number;
    /** data-id for the overflow menu trigger */
    overflowDataId: string;
    /** data-id for the close button */
    closeDataId: string;
    /** Called when the close button is clicked */
    onClose?: () => void;
    /** Additional CSS classes */
    className?: string;
}
export declare function BulkActionMenu({ actions, labels, maxVisibleActions, overflowDataId, closeDataId, onClose, className, 'data-id': dataId, ...rest }: BulkActionMenuProps): import("react/jsx-runtime").JSX.Element;
