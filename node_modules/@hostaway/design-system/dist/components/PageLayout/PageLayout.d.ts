import { PageHeaderProps } from '../PageHeader';
export interface TabItem {
    label: string;
    id: string;
}
export interface PageLayoutBaseProps {
    children: React.ReactNode;
    title?: string;
    tabs?: TabItem[];
    selectedTab?: TabItem['id'];
    onTabChange?: (tabId: string) => void;
    variant?: PageHeaderProps['variant'];
    headerActions?: React.ReactNode;
    tabsSize?: 'sm' | 'md';
    withDivider?: boolean;
}
export interface PageLayoutProps extends PageLayoutBaseProps, Omit<React.ComponentProps<'div'>, 'children' | 'title'> {
}
export declare function PageLayout({ children, title, tabs, selectedTab, onTabChange, variant, headerActions, tabsSize, withDivider, className, ...rest }: PageLayoutProps): import("react/jsx-runtime").JSX.Element;
