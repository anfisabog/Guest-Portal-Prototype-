import type { DatePickerProps as AriaDatePickerProps, DateValue } from 'react-aria-components';
import { WithDataId } from '../../../types/withDataId';
import { DEFAULT_TEXTS } from './defaultTexts';
import { FieldWrapperProps } from '../../molecules/FieldWrapper/FieldWrapper';
export type DatePickerTexts = Pick<typeof DEFAULT_TEXTS, 'placeholder' | 'cancelBtn' | 'applyBtn'>;
export interface DatePickerProps extends AriaDatePickerProps<DateValue>, Omit<FieldWrapperProps, 'children'>, WithDataId {
    /** Custom formatter for displaying the selected date in the trigger button. */
    formatDate?: (date: Date) => string;
    /** The function to call when the apply button is clicked. */
    onApply?: () => void;
    /** The function to call when the cancel button is clicked. */
    onCancel?: () => void;
    /** Override default UI texts for i18n. */
    t?: DatePickerTexts;
    /** Override the default local timezone. */
    timeZone?: string;
    /** @deprecated Use fieldSize instead */
    size?: 'full';
}
export declare const DatePicker: ({ value: valueProp, defaultValue, onChange, onApply, onCancel, formatDate, t: textsProp, timeZone, label, hint, isRequired, isInvalid, "data-id": dataId, fieldSize, size, labelPosition, tooltip, description, id, ...props }: DatePickerProps) => import("react/jsx-runtime").JSX.Element;
