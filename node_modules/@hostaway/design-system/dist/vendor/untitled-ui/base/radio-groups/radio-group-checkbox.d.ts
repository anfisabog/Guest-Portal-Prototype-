import type { FC } from "react";
import type { RadioGroupProps } from "react-aria-components";
import { WithDataId } from '../../../../types/withDataId';
type RadioGroupItemType = {
    value: string;
    title: string;
    disabled?: boolean;
    description: string;
    secondaryTitle: string;
    icon: FC<{
        className?: string;
    }>;
};
interface RadioGroupCheckboxProps extends RadioGroupProps, WithDataId {
    size?: "sm";
    items: RadioGroupItemType[];
}
export declare const RadioGroupCheckbox: ({ items, size, className, "data-id": dataId, ...props }: RadioGroupCheckboxProps) => import("react/jsx-runtime").JSX.Element;
export {};
