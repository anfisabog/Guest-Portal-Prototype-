import type { FC, ReactNode } from "react";
import type { RadioGroupProps } from "react-aria-components";
import { WithDataId } from '../../../../types/withDataId';
type IconCardItemType = {
    value: string;
    title: string;
    description: string;
    secondaryTitle?: string;
    disabled?: boolean;
    price?: string;
    badge?: ReactNode;
    icon: FC<{
        className?: string;
    }>;
};
interface RadioGroupIconCardProps extends RadioGroupProps, WithDataId {
    size?: "sm";
    items: IconCardItemType[];
}
export declare const RadioGroupIconCard: ({ items, size, className, "data-id": dataId, ...props }: RadioGroupIconCardProps) => import("react/jsx-runtime").JSX.Element;
export {};
