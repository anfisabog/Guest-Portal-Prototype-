import type { RadioGroupProps } from "react-aria-components";
import { WithDataId } from '../../../../types/withDataId';
interface AvatarItemType {
    id: string;
    name: string;
    username: string;
    title: string;
    avatarUrl: string;
    disabled?: boolean;
}
interface RadioGroupAvatarProps extends RadioGroupProps, WithDataId {
    size?: "sm";
    items: AvatarItemType[];
}
export declare const RadioGroupAvatar: ({ items, size, className, "data-id": dataId, ...props }: RadioGroupAvatarProps) => import("react/jsx-runtime").JSX.Element;
export {};
