import { type ButtonProps as AriaButtonProps } from "react-aria-components";
interface CloseButtonProps extends AriaButtonProps {
    theme?: "light" | "dark";
    size?: "sm" | "lg";
    label?: string;
}
export declare const CloseButton: ({ label, className, size, theme, ...otherProps }: CloseButtonProps) => import("react/jsx-runtime").JSX.Element;
export {};
