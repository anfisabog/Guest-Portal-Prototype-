import type { ReactNode } from "react";
type HeaderNavItem = {
    label: string;
    href?: string;
    menu?: ReactNode;
};
interface HeaderProps {
    items?: HeaderNavItem[];
    isFullWidth?: boolean;
    isFloating?: boolean;
    className?: string;
}
export declare const Header: ({ items, isFullWidth, isFloating, className }: HeaderProps) => import("react/jsx-runtime").JSX.Element;
export {};
