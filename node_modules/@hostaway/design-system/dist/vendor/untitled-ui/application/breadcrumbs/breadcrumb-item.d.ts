import { type FC, type ReactNode } from "react";
import { type BreadcrumbProps as AriaBreadcrumbProps } from "react-aria-components";
import { type BreadcrumbType } from './breadcrumbs';
import { WithDataId } from '../../../../types/withDataId';
export interface BreadcrumbItemProps extends AriaBreadcrumbProps, WithDataId {
    href?: string;
    divider?: "chevron" | "slash";
    type?: BreadcrumbType;
    isEllipsis?: boolean;
    children?: ReactNode;
    icon?: FC<{
        className?: string;
    }> | ReactNode;
    onClick?: () => void;
}
export declare const BreadcrumbItem: ({ href, icon, divider, type, isEllipsis, children, onClick, "data-id": dataId, ...otherProps }: BreadcrumbItemProps) => import("react/jsx-runtime").JSX.Element;
