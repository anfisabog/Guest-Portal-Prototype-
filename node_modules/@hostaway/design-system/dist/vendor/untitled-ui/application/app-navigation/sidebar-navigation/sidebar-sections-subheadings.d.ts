import type { NavItemType } from "../config";
import type { WithDataId } from '../../../../../types/withDataId';
interface SidebarNavigationSectionsSubheadingsProps extends WithDataId {
    /** URL of the currently active item. */
    activeUrl?: string;
    /** List of items to display. */
    items: Array<{
        label: string;
        items: NavItemType[];
    }>;
}
export declare const SidebarNavigationSectionsSubheadings: ({ activeUrl, items, "data-id": dataId }: SidebarNavigationSectionsSubheadingsProps) => import("react/jsx-runtime").JSX.Element;
export {};
