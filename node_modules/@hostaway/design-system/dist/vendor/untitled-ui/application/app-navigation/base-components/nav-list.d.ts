import type { WithDataId } from '../../../../../types/withDataId';
import type { NavItemDividerType, NavItemType } from "../config";
export interface NavListProps extends WithDataId {
    /** URL of the currently active item. */
    activeUrl?: string;
    /** Additional CSS classes to apply to the list. */
    className?: string;
    /** List of items to display. */
    items: (NavItemType | NavItemDividerType)[];
}
export declare const NavList: ({ activeUrl, items, className, "data-id": dataId }: NavListProps) => import("react/jsx-runtime").JSX.Element;
