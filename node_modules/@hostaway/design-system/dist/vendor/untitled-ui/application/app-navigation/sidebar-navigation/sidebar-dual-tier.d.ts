import type { ReactNode } from "react";
import type { NavItemType } from "../config";
import type { WithDataId } from '../../../../../types/withDataId';
interface SidebarNavigationDualTierProps extends WithDataId {
    /** URL of the currently active item. */
    activeUrl?: string;
    /** Feature card to display. */
    featureCard?: ReactNode;
    /** List of items to display. */
    items: NavItemType[];
    /** List of footer items to display. */
    footerItems?: NavItemType[];
    /** Whether to hide the right side border. */
    hideBorder?: boolean;
}
export declare const SidebarNavigationDualTier: ({ activeUrl, hideBorder, items, footerItems, featureCard, "data-id": dataId }: SidebarNavigationDualTierProps) => import("react/jsx-runtime").JSX.Element;
export {};
